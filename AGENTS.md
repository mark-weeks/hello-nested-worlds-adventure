# Agent entry point

The authoritative working instructions for this repository are in
[`CLAUDE.md`](CLAUDE.md). Read that file completely before planning or changing
code; its world covenants, one-way-door rules, determinism contract, testing
discipline, CHANGELOG requirement, ADR process, and irreversibility check apply
to every coding agent, not only Claude-branded tools.

Use `./setup.sh` for a pinned Python 3.11 / Node 20 environment and
`./scripts/check.sh` for the canonical local verification sequence.

Do not duplicate those instructions here. When a working rule changes, update
`CLAUDE.md`; keep this file as the cross-agent discovery pointer.
