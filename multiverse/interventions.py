"""Current proposal vocabulary; accepted work selects its retained interpreter."""
from multiverse.interventions_v1 import (
    VERSION as VERSION, MAX_STEPS as MAX_STEPS, OPERATORS as OPERATORS,
    digest as digest, read_state as read_state, normalize_steps as normalize_steps,
    parse_score as parse_score, simulate as simulate, receive as receive,
    suggestions as suggestions,
)
